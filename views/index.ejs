<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/styles.css" />
    <title><%= title %></title>
  </head>
  <body>
    <div class="page">
      <header class="page-header">
        <h1><%= title %></h1>
        <a class="button" href="/new">Add new message</a>
      </header>

      <main class="stack">
        <% for (const message of messages) { %>
          <article class="card message-card">
            <h2 class="message-author"><%= message.user %></h2>
            <p class="message-text"><%= message.text %></p>
            <p class="message-date"><%= message.added.toLocaleString() %></p>
            <a class="link-chip" href="/messages/<%= message.id %>">Open</a>
          </article>
        <% } %>
      </main>
    </div>
  </body>
</html>
